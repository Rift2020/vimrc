/**
 *    author:  Rift
 *    created: 2021.12.23  22:16
**/
#include<bits/stdc++.h>
#define rep(i, a, b) for (int i = (a); i <= (b); ++i)
#define per(i, a, b) for (int i = (a); i >= (b); --i)
#ifdef LOCAL
#define cout cout<<"\033[32m"
#define endl "\033[0m"<<endl
#endif
using namespace std;
using ll = long long;

signed main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr);



	#ifdef LOCAL
	fflush(stdin);getchar();
	#endif
	return 0;
}
